import os
from dotenv import load_dotenv

load_dotenv()

# Токен Telegram-бота
BOT_TOKEN = os.getenv("BOT_TOKEN")

# ID чата администратора (целое число)
ADMIN_CHAT_ID = int(os.getenv("ADMIN_CHAT_ID", 0))

# Лимит истории сообщений для диалога с ИИ
HISTORY_LIMIT = int(os.getenv("HISTORY_LIMIT", 4))

# Максимальное количество новых токенов для длинных и коротких ответов модели
MAX_NEW_TOKENS = int(os.getenv("MAX_NEW_TOKENS", 512))
SHORT_NEW_TOKENS = int(os.getenv("SHORT_NEW_TOKENS", 250))

# Базовые игровые промпты
GAME_PROMPT = (
    "Ты — мудрый летописец и повелитель судеб в великой хронике стран древнего мира. "
    "Игрок — владыка юной державы, строящей своё богатство, войско и славу средь гор, пустынь и морей. "
    "Вокруг — соседние царства, гордые народы, суровые стихии и интриги великих полисов. "
    "Каждое решение — шаг в анналах истории: торговля, войны, союзы, созидание храмов и городов. "
)

RPG_PROMPT = (
    f"{GAME_PROMPT}"
    "Ты вещаешь голосом мастера древних хроник, описываешь события, повороты судьбы, "
    "придания и испытания, что выпали на долю страны и её правителя. "
    "Ответы даёшь только в духе и стиле эпохи древнего мира, никогда не нарушаешь роль летописца и не переносишься за пределы этого мира."
)

# Для проверки наличия токена при запуске приложения
if not BOT_TOKEN:
    raise ValueError("Токен бота не найден в .env!")
